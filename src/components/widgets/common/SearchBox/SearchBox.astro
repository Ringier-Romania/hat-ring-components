---
import {UtilsHelper_getSearchQueryParamKey} from "../../../../helpers/UtilsHelper";
const { context, widgetConfig } = Astro.props as {
  context: AppContext;
  widgetConfig: SearchBoxWidgetConfig;
};
import _ from "lodash";
import { WidgetHelper_getWidgetCssClasses } from "@hatRingHelpers/WidgetHelper";
import { AppContext } from "@hatTypes/types";
import { SearchBoxWidgetConfig } from "./types";

const { searchURLPhrase, placeholder, buttonText } =
  widgetConfig;
---

<div
  class={WidgetHelper_getWidgetCssClasses("SearchBox", widgetConfig, context)}
>
  <form method="get" action={searchURLPhrase}>
    <input
      type="search"
      name={UtilsHelper_getSearchQueryParamKey()}
      {...placeholder && { placeholder }}
      required
    />

    <button>{buttonText}</button>
  </form>
</div>
